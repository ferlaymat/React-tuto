// 1 setup Files —to avoid the need to import these configs in all test components
import '@testing-library/jest-dom'; // ← executed before tests. Add more matcher patterns

// boardReducer.test.ts  → nothing to import
// taskService.test.ts   → nothing to import
// TaskCard.test.tsx     → nothing to import